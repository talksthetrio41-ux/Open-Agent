# Termux / Android — NO Playwright.
# Playwright publishes no wheels for aarch64-linux-android and its
# driver is a glibc binary that cannot run on Bionic. The agent drives
# Termux's system Chromium over Chrome DevTools Protocol instead.
httpx>=0.27.0
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
sse-starlette>=2.0.0
python-dotenv>=1.0.0
aiofiles>=23.2.1
websockets>=12.0
pytest>=8.0.0
